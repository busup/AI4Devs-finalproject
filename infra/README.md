# Infraestructura

Este directorio está reservado para configuración de infraestructura compartida (scripts de despliegue, CI/CD, configuración de entornos, etc.) cuando se requiera.

En desarrollo local, la orquestación se realiza mediante el `docker-compose.yml` de la raíz del repositorio.
