import type { SearchRoutesResponse } from '@/types'

/** Respuesta mínima para desarrollo cuando VITE_USE_MOCK_API=true */
export const minimalSearchRoutesResponse: SearchRoutesResponse = {
  results: [],
}
